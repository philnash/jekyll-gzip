source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in jekyll-gzip.gemspec
gemspec

jekyll_version = ENV["JEKYLL_VERSION"] || "~> 4.0"

gem "jekyll", jekyll_version